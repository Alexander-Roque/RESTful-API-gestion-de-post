Nuestra base de datos llama gestion_post

dentro de la base de datos existen 3 tablas;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username varchar(255) NOT NULL UNIQUE,
    password varchar(60) NOT NULL,
    email varchar(60) NOT NULL UNIQUE,
    firstName varchar(100) NULL,
    lastName varchar(100) NULL,
    role varchar(60) NOT NULL DEFAULT 'user',
    createdAt TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updatedAt TIMESTAMPTZ NOT NULL DEFAULT NOW(),
);

CREATE TABLE posts (
    id SERIAL PRIMARY KEY,
    userId INTEGER NOT NULL REFERENCES users(id),
    content TEXT NOT NULL,
    createdAt TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updatedAt TIMESTAMPTZ NOT NULL DEFAULT NOW(),
);

CREATE TABLE likes (
    id SERIAL PRIMARY KEY,
    postId INTEGER NOT NULL REFERENCES posts(id) ON DELETE CASCADE,
    userId INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    createdAt TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    
    //evitamos likes duplicados
    UNIQUE (postId, userId)
);

\dt  las tablas ya estan creadas

psql -U postgres -h localhost -p 5432
\c gestion_post

vamos hacer una prueba 
En mi primera tabla users colocamos 
INSERT INTO users (username, email, firstname, lastname, role, createdAt, updatedAt)
VALUES ('alex', 'alex@gmail.com', 'Alex', 'Gomez', 'user', NOW(), NOW());

VALUES ('jose', 'jose@gmail.com', 'Jose', 'Roque', 'user', NOW(), NOW());


en la segunda tabla posts
INSERT INTO posts (userId, content, createdAt, updatedAt)
VALUES (1, 'Este es mi primer post', NOW(), NOW());

VALUES (2, 'Este es mi segundo post', NOW(), NOW());

En mi ultima tabla likes colocamos;
INSERT INTO likes (postId, userId, createdAt)
VALUES (1, 1, NOW());

VALUES (2, 2, NOW());